import 'package:flutter/material.dart';

class siteSeen extends StatelessWidget {
  const siteSeen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:
      AppBar(title: Text("My site first view"),),
    );
  }
}
